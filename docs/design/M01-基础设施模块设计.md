# M01: 基础设施模块 (waleo.utils) 设计文档

> **优先级**: P0 (核心基础)
> **依赖**: 无
> **状态**: 设计中

---

## 1. 模块概述

**模块名称**: `waleo.utils`
**版本**: v0.1.0
**描述**: 提供机器人学习框架所需的基础工具函数和常量定义，是所有其他模块的依赖基础。

---

## 2. 模块职责

### 2.1 核心职责
- **设备分层管理**：训练设备（CUDA+NCCL） vs 推理设备（CUDA/MPS/CPU）
- **分布式训练支持**：NCCL 单机多卡/多机多卡通信、进程组初始化
- **通信基础设施**：RPC 通信框架、序列化工具、客户端/服务端基类
- **日志记录**：格式化输出、文件日志、分布式日志
- **随机数管理**：种子设置、状态保存/恢复、分布式随机数同步
- **时间测量**：性能计时器、同步点测量
- **文件 I/O**：视频保存、JSON 序列化
- **常量定义**：路径、键名、环境变量

### 2.2 非职责
- 不涉及任何深度学习模型逻辑
- 不涉及机器人硬件控制细节
- 不涉及数据处理（属于 M03 数据集模块）
- 不涉及分布式训练的具体训练循环（属于 M13 训练模块）
- 不涉及具体的通信协议实现（属于 M21 通信服务模块）

---

## 3. 子模块结构

```
waleo.utils/
├── waleo.utils/
│   ├── __init__.py
│   ├── device/                  # 设备管理
│   │   ├── __init__.py
│   │   ├── manager.py           # 设备选择和管理
│   │   └── distributed.py       # 分布式设备（NCCL，支持多机多卡）
│   ├── distributed/             # 分布式训练支持
│   │   ├── __init__.py
│   │   ├── launcher.py          # 多进程启动
│   │   ├── reducer.py           # 梯度同步
│   │   └── state.py             # 分布式状态管理
│   ├── communication/           # 通信基础设施
│   │   ├── __init__.py
│   │   ├── base.py              # RPC 基类
│   │   ├── client.py            # 客户端基类
│   │   ├── server.py            # 服务端基类
│   │   ├── serialization.py     # 序列化工具
│   │   └── protocol.py          # 协议定义
│   ├── logging/                 # 日志记录
│   │   ├── __init__.py
│   │   ├── formatter.py         # 格式化器
│   │   └── metrics.py           # 指标追踪
│   ├── random/                  # 随机数管理
│   │   ├── __init__.py
│   │   └── state.py             # RNG 状态管理
│   ├── timing/                  # 时间测量
│   │   ├── __init__.py
│   │   └── timer.py             # 计时器
│   ├── io/                      # 文件I/O
│   │   ├── __init__.py
│   │   ├── video.py             # 视频保存
│   │   └── json.py              # JSON 序列化
│   └── constants.py              # 常量定义
├── tests/
│   ├── test_device.py
│   ├── test_distributed.py
│   ├── test_communication.py
│   ├── test_logging.py
│   ├── test_random.py
│   ├── test_timing.py
│   └── test_io.py
├── pyproject.toml
└── README.md
```

---

## 4. API 设计

### 4.1 设备管理 (`device/`)

#### 4.1.1 基础设备管理 (`manager.py`)

**设计理念**：根据使用场景分层选择设备

| 函数名 | 输入 | 输出 | 描述 |
|--------|------|------|------|
| `get_training_device()` | 无 | `torch.device` | 获取训练设备（仅 CUDA） |
| `get_inference_device()` | 无 | `torch.device` | 获取推理设备（CUDA > MPS > CPU） |
| `get_device(device_str)` | `str` | `torch.device` | 根据字符串获取设备 |
| `is_device_available(device_str)` | `str` | `bool` | 检查设备是否可用 |
| `get_device_count(device_type)` | `str` | `int` | 获取指定类型的设备数量 |
| `get_device_capabilities()` | 无 | `dict` | 获取设备能力信息 |

#### 使用示例

```python
from waleo.utils.device import get_training_device, get_inference_device, get_device_capabilities

# 训练：只使用 CUDA
train_device = get_training_device()  # cuda:0 或抛出异常

# 推理：可以使用任何设备
infer_device = get_inference_device()  # cuda/mps/cpu

# 获取设备能力
caps = get_device_capabilities()
# {
#     'cuda': {'available': True, 'count': 4, 'nccl': True},
#     'mps': {'available': True, 'count': 1, 'nccl': False},
#     'cpu': {'available': True}
# }
```

#### 4.1.2 分布式设备 (`distributed.py`)

**设计理念**：支持单机多卡和多机多卡，使用统一接口

| 函数名 | 输入 | 输出 | 描述 |
|--------|------|------|------|
| `setup_distributed(backend="nccl")` | `str` | 无 | 初始化分布式环境 |
| `setup_distributed_multinode(init_method, world_size, rank)` | `str, int, int` | 无 | 初始化多机分布式 |
| `get_world_size()` | 无 | `int` | 获取进程总数 |
| `get_rank()` | 无 | `int` | 获取当前进程 rank |
| `get_local_rank()` | 无 | `int` | 获取本地进程 rank |
| `is_main_process()` | 无 | `bool` | 是否为主进程 |
| `barrier()` | 无 | 无 | 同步所有进程 |
| `destroy_distributed()` | 无 | 无 | 清理分布式环境 |

#### 使用示例

```python
from waleo.utils.device import setup_distributed, get_rank, get_world_size

# 单机多卡（自动检测）
setup_distributed(backend="nccl")

# 多机多卡（手动指定）
# Node 0:
setup_distributed_multinode(
    init_method="tcp://192.168.1.1:12345",
    world_size=8,  # 2 nodes × 4 GPUs
    rank=0
)

# Node 1:
setup_distributed_multinode(
    init_method="tcp://192.168.1.1:12345",
    world_size=8,
    rank=4  # offset by node 0's GPUs
)

rank = get_rank()
world_size = get_world_size()
device = torch.device(f"cuda:{get_local_rank()}")
```

#### 依赖
- `torch`
- `torch.distributed`
- 无其他 waleo 模块依赖

---

### 4.2 分布式训练支持 (`distributed/`)

#### 4.2.1 多进程启动 (`launcher.py`)

| 函数名 | 输入 | 输出 | 描述 |
|--------|------|------|------|
| `launch_multi_process(num_gpus, func, args)` | `int, callable, tuple` | 无 | 启动多进程训练 |
| `launch_multinode(nodes_config, func, args)` | `list[dict], callable, tuple` | 无 | 启动多机多卡训练 |
| `run_main(rank, world_size, func, args)` | `int, int, callable, tuple` | 无 | 单进程主函数 |

#### 使用示例

```python
from waleo.utils.distributed import launch_multi_process, launch_multinode

def train_worker(rank, world_size, config):
    """每个进程的训练函数"""
    setup_distributed()
    device = torch.device(f"cuda:{rank}")
    model = MyModel().to(device)
    model = torch.nn.parallel.DistributedDataParallel(
        model, device_ids=[rank]
    )
    # 训练...

# 单机多卡
launch_multi_process(num_gpus=4, func=train_worker, args=(config,))

# 多机多卡
nodes_config = [
    {"host": "node0", "num_gpus": 4},
    {"host": "node1", "num_gpus": 4},
]
launch_multinode(nodes_config, func=train_worker, args=(config,))
```

#### 4.2.2 梯度同步 (`reducer.py`)

| 函数名 | 输入 | 输出 | 描述 |
|--------|------|------|------|
| `all_reduce(tensor, op="avg")` | `tensor, str` | `tensor` | 跨进程归约张量 |
| `broadcast(tensor, src=0)` | `tensor, int` | `tensor` | 从主进程广播张量 |
| `all_gather(tensors)` | `list[tensor]` | `list[tensor]` | 收集所有进程的张量 |

#### 4.2.3 分布式状态 (`state.py`)

| 类名 | 方法 | 描述 |
|------|------|------|
| `DistributedState` | `__init__()` | 管理分布式状态 |
| | `@property rank` | 当前 rank |
| | `@property world_size` | 总进程数 |
| | `@property local_rank` | 本地 rank |

#### 依赖
- `torch`
- `torch.distributed`
- `torch.multiprocessing`
- `waleo.utils.device` (M01)

---

### 4.3 通信基础设施 (`communication/`) ← 新增

**设计理念**：提供通用的 RPC 通信框架，支持远程服务调用

#### 4.3.1 RPC 基类 (`base.py`)

| 类名 | 方法 | 描述 |
|------|------|------|
| `RPCEndpoint` | `__init__(host, port)` | RPC 端点基类 |
| | `start()` | 启动服务 |
| | `stop()` | 停止服务 |
| | `is_running()` | 检查运行状态 |

#### 4.3.2 客户端基类 (`client.py`)

```python
class RPCClient:
    """RPC 客户端基类

    提供同步/异步远程调用能力
    """

    def __init__(self, host: str, port: int, timeout: float = 5.0):
        """
        Args:
            host: 服务端地址
            port: 服务端端口
            timeout: 超时时间
        """
        ...

    def connect(self) -> bool:
        """连接到服务端"""
        ...

    def disconnect(self) -> None:
        """断开连接"""
        ...

    def is_connected(self) -> bool:
        """检查连接状态"""
        ...

    def call(self, method: str, *args, **kwargs) -> Any:
        """同步调用远程方法"""
        ...

    async def call_async(self, method: str, *args, **kwargs) -> Any:
        """异步调用远程方法"""
        ...

    def call_stream(self, method: str, *args, **kwargs) -> Iterator:
        """流式调用（返回迭代器）"""
        ...
```

#### 4.3.3 服务端基类 (`server.py`)

```python
class RPCServer:
    """RPC 服务端基类

    提供远程方法注册和调用
    """

    def __init__(self, host: str = "0.0.0.0", port: int = 5000, max_workers: int = 4):
        """
        Args:
            host: 绑定地址
            port: 绑定端口
            max_workers: 最大工作线程数
        """
        ...

    def register_method(self, name: str, func: callable):
        """注册可远程调用的方法"""
        ...

    def start(self, block: bool = True):
        """启动服务"""
        ...

    def stop(self):
        """停止服务"""
        ...

    def is_running(self) -> bool:
        """检查运行状态"""
        ...
```

#### 4.3.4 序列化工具 (`serialization.py`)

| 函数名 | 输入 | 输出 | 描述 |
|--------|------|------|------|
| `serialize(obj)` | `Any` | `bytes` | 序列化对象（支持 tensor、numpy 等） |
| `deserialize(data)` | `bytes` | `Any` | 反序列化对象 |
| `serialize_tensor(tensor)` | `torch.Tensor` | `bytes` | 序列化张量 |
| `deserialize_tensor(data)` | `bytes` | `torch.Tensor` | 反序列化张量 |

#### 4.3.5 协议定义 (`protocol.py`)

```python
from enum import Enum

class MessageType(Enum):
    """消息类型"""
    REQUEST = "request"
    RESPONSE = "response"
    STREAM_START = "stream_start"
    STREAM_CHUNK = "stream_chunk"
    STREAM_END = "stream_end"
    ERROR = "error"

class ErrorCode(Enum):
    """错误码"""
    OK = 0
    NOT_FOUND = 1
    INVALID_REQUEST = 2
    INTERNAL_ERROR = 3
    TIMEOUT = 4

@dataclass
class RPCMessage:
    """RPC 消息格式"""
    msg_type: MessageType
    msg_id: str
    method: str | None = None
    args: tuple | None = None
    kwargs: dict | None = None
    result: Any | None = None
    error: ErrorCode | None = None
    error_msg: str | None = None
```

#### 使用示例

```python
from waleo.utils.communication import RPCClient, RPCServer, serialize, deserialize

# 服务端
class MyService(RPCServer):
    def __init__(self):
        super().__init__(host="0.0.0.0", port=5000)
        self.register_method("predict", self.predict)
        self.register_method("process_stream", self.process_stream)

    def predict(self, inputs: dict) -> dict:
        """推理方法"""
        # 处理输入并返回结果
        return {"action": [0.1, 0.2, 0.3]}

    def process_stream(self, inputs: dict):
        """流式处理"""
        for i in range(10):
            yield {"step": i, "value": i * 0.1}

# 启动服务
service = MyService()
service.start()

# 客户端
client = RPCClient(host="localhost", port=5000)
client.connect()

# 同步调用
result = client.call("predict", inputs={"obs": [...]})
print(result)  # {"action": [0.1, 0.2, 0.3]}

# 异步调用
result = await client.call_async("predict", inputs={"obs": [...]})

# 流式调用
for chunk in client.call_stream("process_stream", inputs={"data": [...]}):
    print(chunk)  # {"step": 0, "value": 0.0}, {"step": 1, "value": 0.1}, ...
```

#### 依赖
- `asyncio` (标准库)
- `socket` (标准库)
- `pickle` (标准库)
- `torch` (张量序列化)
- `numpy` (数组序列化)

---

### 4.4 日志记录 (`logging/`)

#### 4.4.1 格式化器 (`formatter.py`)

| 函数名 | 输入 | 输出 | 描述 |
|--------|------|------|------|
| `init_logging(log_file=None, level="INFO")` | `Path, str` | 无 | 初始化日志系统 |
| `get_logger(name)` | `str` | `Logger` | 获取命名日志记录器 |

#### 日志格式
```
INFO 2026-01-08 15:30:45    manager.py:120 Device cuda is selected
```

#### 4.5.2 指标追踪 (`metrics.py`)

| 类名 | 方法 | 描述 |
|------|------|------|
| `AverageMeter` | `update(val, n=1)` | 更新平均值 |
| | `reset()` | 重置 |
| | `@property avg` | 获取平均值 |
| | `@property sum` | 获取总和 |
| `MetricsTracker` | `step()` | 增加步数 |
| | `to_dict()` | 导出为字典 |
| | `reset_averages()` | 重置平均值 |

---

### 4.5 随机数管理 (`random/state.py`)

| 函数名 | 输入 | 输出 | 描述 |
|--------|------|------|------|
| `set_seed(seed)` | `int` | 无 | 设置 random、numpy、torch 的随机种子 |
| `get_rng_state()` | 无 | `dict` | 获取所有 RNG 状态 |
| `set_rng_state(state_dict)` | `dict` | 无 | 恢复所有 RNG 状态 |
| `save_rng_state(save_dir)` | `Path` | 无 | 保存 RNG 状态到文件 |
| `load_rng_state(save_dir)` | `Path` | 无 | 从文件加载 RNG 状态 |
| `seeded_context(seed)` | `int` | Context Manager | 临时设置种子的上下文管理器 |
| `sync_seed_across_processes(seed)` | `int` | 无 | 在分布式环境中同步种子 |

---

### 4.6 时间测量 (`timing/timer.py`)

| 类名 | 方法/属性 | 描述 |
|------|-----------|------|
| `TimerManager` | `__enter__()` / `__exit__()` | 上下文管理器支持 |
| | `start()` / `stop()` | 手动控制 |
| | `@property last` | 最后一次耗时（秒） |
| | `@property avg` | 平均耗时 |
| | `@property fps_last` | 最后一次 FPS |
| | `@property fps_avg` | 平均 FPS |
| | `percentile(p)` | 第 p 百分位耗时 |
| | `reset()` | 清空历史 |

---

### 4.7 文件 I/O (`io/`)

#### 4.7.1 视频保存 (`video.py`)

| 函数名 | 输入 | 输出 | 描述 |
|--------|------|------|------|
| `write_video(path, frames, fps)` | `Path, ndarray, int` | 无 | 保存图像序列为 MP4 视频 |

#### 4.7.2 JSON 序列化 (`json.py`)

| 函数名 | 输入 | 输出 | 描述 |
|--------|------|------|------|
| `deserialize_json_into_object(path, obj)` | `Path, T` | `T` | 从 JSON 文件加载并填充对象 |

---

### 4.8 常量定义 (`constants.py`)

```python
# ============================================================================
# 观察和动作键名
# ============================================================================
OBS_ENV_STATE = "observation.environment_state"
OBS_STATE = "observation.state"
OBS_IMAGE = "observation.image"
OBS_IMAGES = "observation.images"
ACTION = "action"
REWARD = "next.reward"

# ============================================================================
# 路径常量
# ============================================================================
WALES_HOME = Path("~/.cache/waleo").expanduser()
WALES_CALIBRATION = WALES_HOME / "calibration"

# 训练相关
CHECKPOINTS_DIR = "checkpoints"
PRETRAINED_MODEL_DIR = "pretrained_model"
TRAINING_STATE_DIR = "training_state"

# ============================================================================
# 文件名常量
# ============================================================================
RNG_STATE = "rng_state.safetensors"
TRAINING_STEP = "training_step.json"
OPTIMIZER_STATE = "optimizer_state.safetensors"
OPTIMIZER_PARAM_GROUPS = "optimizer_param_groups.json"
SCHEDULER_STATE = "scheduler_state.json"

# ============================================================================
# 通信常量
# ============================================================================
DEFAULT_RPC_PORT = 5000
DEFAULT_RPC_TIMEOUT = 5.0
MAX_MESSAGE_SIZE = 100 * 1024 * 1024  # 100MB

# ============================================================================
# 仿真常量
# ============================================================================
DEFAULT_FPS = 30
DEFAULT_MAX_EPISODE_STEPS = 1000
```

---

## 5. 模块接口

### 5.1 公开 API (`__init__.py`)

```python
# 设备管理
from waleo.utils.device import (
    get_training_device,
    get_inference_device,
    get_device,
    is_device_available,
    get_device_count,
    get_device_capabilities,
    setup_distributed,
    setup_distributed_multinode,
    get_world_size,
    get_rank,
    get_local_rank,
    is_main_process,
    barrier,
    destroy_distributed,
)

# 分布式训练
from waleo.utils.distributed import (
    launch_multi_process,
    launch_multinode,
    run_main,
)

# 通信基础设施
from waleo.utils.communication import (
    RPCClient,
    RPCServer,
    RPCEndpoint,
    serialize,
    deserialize,
    serialize_tensor,
    deserialize_tensor,
    MessageType,
    ErrorCode,
    RPCMessage,
)

# 日志记录
from waleo.utils.logging import (
    init_logging,
    get_logger,
    AverageMeter,
    MetricsTracker,
)

# 随机数管理
from waleo.utils.random import (
    set_seed,
    get_rng_state,
    set_rng_state,
    save_rng_state,
    load_rng_state,
    seeded_context,
    sync_seed_across_processes,
)

# 时间测量
from waleo.utils.timing import TimerManager

# 文件 I/O
from waleo.utils.io import (
    write_video,
    deserialize_json_into_object,
)

# 常量
from waleo.utils.constants import (
    # 观察和动作键名
    OBS_ENV_STATE,
    OBS_STATE,
    OBS_IMAGE,
    OBS_IMAGES,
    ACTION,
    REWARD,
    # 路径
    WALES_HOME,
    WALES_CALIBRATION,
    CHECKPOINTS_DIR,
    PRETRAINED_MODEL_DIR,
    TRAINING_STATE_DIR,
    # 文件名
    RNG_STATE,
    TRAINING_STEP,
    OPTIMIZER_STATE,
    OPTIMIZER_PARAM_GROUPS,
    SCHEDULER_STATE,
    # 通信
    DEFAULT_RPC_PORT,
    DEFAULT_RPC_TIMEOUT,
    MAX_MESSAGE_SIZE,
)
```

---

## 6. 与其他模块的接口

### 6.1 依赖关系

| 依赖模块 | 使用的功能 | 用途 |
|----------|------------|------|
| M02 配置管理 | `get_training_device`, `init_logging` | 设备选择和日志初始化 |
| M03 数据集 | `WALES_HOME`, `write_video` | 缓存路径和视频保存 |
| M04 策略接口 | `get_training_device`, `set_seed` | 设备和可复现性 |
| M07 电机控制 | `init_logging`, `TimerManager` | 日志和性能测量 |
| M13 训练脚本 | `TimerManager` | 训练速度监控 |
| M21 通信服务 | `RPCClient`, `RPCServer` | 通信基类 |

### 6.2 被依赖关系

| 依赖模块 | 使用的功能 | 用途 |
|----------|------------|------|
| M04 策略模块 | 分布式训练、通信 | 多GPU训练、远程推理 |
| M13 训练脚本 | 设备管理、日志、计时 | 训练流程 |
| M21 通信服务 | RPC 基类 | 机器人通信 |

---

## 7. 数据流和架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        应用层                                   │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌──────────┐  │
│  │ 训练脚本    │  │ 评估脚本    │  │ 仿真训练    │  │ 远程推理  │  │
│  └─────┬──────┘  └─────┬──────┘  └─────┬──────┘  └────┬─────┘  │
└────────┼───────────────┼───────────────┼───────────────┼────────┘
         │               │               │               │
         ▼               ▼               ▼               ▼
┌─────────────────────────────────────────────────────────────────┐
│                    M01: waleo.utils 核心层                    │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐             │  │
│  │  │  Device   │  │  Dist.    │  │  Comm.    │             │  │
│  │  │  Manager  │  │  Training │  │  Base     │             │  │
│  │  └──────────┘  └──────────┘  └──────────┘             │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐             │  │
│  │  │  Logging │  │  Random   │  │  Timing   │             │  │
│  │  └──────────┘  └──────────┘  └──────────┘             │  │
│  │  ┌──────────┐                                           │  │
│  │  │   I/O     │                                           │  │
│  │  └──────────┘                                           │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
         │                │                │
         ▼                ▼                ▼
┌─────────────────────────────────────────────────────────────────┐
│                     其他模块（M02-M22）                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 8. 异步推理架构

```
┌─────────────────────────────────────────────────────────────┐
│              CUDA 训练/推理服务器                             │
│  ┌────────────────────────────────────────────────────┐    │
│  │  分布式训练 (NCCL 单机/多机多卡)                       │    │
│  │  训练策略模型                                          │    │
│  │  模型推理服务                                          │    │
│  └───────────────────┬────────────────────────────────┘    │
│                      │ 网络提供推理服务                         │
└──────────────────────┼──────────────────────────────────────┘
                       │ RPC/TCP
┌──────────────────────┼──────────────────────────────────────┐
│                      ▼                                       │
│  ┌────────────────────────────────────────────────────┐    │
│  │          机器人本体 (MPS/CPU)                       │    │
│  │  ┌──────────────────────────────────────────────┐  │    │
│  │  │ 1. 采集观察数据 (observation)                  │  │    │
│  │  └────────────┬─────────────────────────────────┘  │    │
│  │               │                                      │    │
│  │               ▼                                      │    │
│  │  ┌──────────────────────────────────────────────┐  │    │
│  │  │ 2. 发送推理请求 (obs) → 服务器               │  │    │
│  │  └────────────┬─────────────────────────────────┘  │    │
│  │               │                                      │    │
│  │               ▼                                      │    │
│  │  ┌──────────────────────────────────────────────┐  │    │
│  │  │ 3. 接收 action chunk ← 服务器                │  │    │
│  │  └────────────┬─────────────────────────────────┘  │    │
│  │               │                                      │    │
│  │               ▼                                      │    │
│  │  ┌──────────────────────────────────────────────┐  │    │
│  │  │ 4. 执行 action chunk (future N steps)       │  │    │
│  │  └──────────────────────────────────────────────┘  │    │
│  └────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

**关键点**：
- 机器人本体只需要 MPS/CPU（低算力）
- 训练和推理在 CUDA 服务器完成
- 通过 RPC 通信异步请求推理
- 机器人本体只负责：采集 → 请求 → 执行

---

## 9. 依赖项

### 9.1 核心依赖（必需）
```toml
[tool.poetry.dependencies]
python = "^3.9"
torch = "^2.0.0"
numpy = "^1.24.0"
safetensors = "^0.4.0"
```

### 9.2 通信依赖（必需）
```toml
aiohttp = "^3.9.0"
```

### 9.3 视频支持依赖（可选）
```toml
[tool.poetry.extras]
video = ["imageio = "^2.31.0"]
```

### 9.5 开发依赖
```toml
[tool.poetry.dev-dependencies]
pytest = "^7.4.0"
pytest-cov = "^4.1.0"
pytest-asyncio = "^0.21.0"
black = "^23.0.0"
mypy = "^1.5.0"
```

---

## 10. 兼容性

### 10.1 Python 版本
- 最低：Python 3.9
- 测试：3.9, 3.10, 3.11

### 10.2 平台支持
- Linux: ✅ 完全支持（推荐，NCCL 支持）
- macOS: ✅ 支持（MPS 推理，无 NCCL）
- Windows: ⚠️ 部分支持（无 CUDA 设备检测）

### 10.3 PyTorch 版本
- 最低：PyTorch 2.0.0
- 推荐：PyTorch 2.1.0+（更好的 NCCL 支持）

---

## 11. 文件大小估算

| 文件/目录 | 估计行数 | 说明 |
|-----------|----------|------|
| `device/manager.py` | ~180 | 设备管理（训练/推理分离） |
| `device/distributed.py` | ~250 | 分布式设备（NCCL 多机多卡） |
| `distributed/launcher.py` | ~180 | 多进程/多机启动 |
| `distributed/reducer.py` | ~120 | 梯度同步 |
| `distributed/state.py` | ~100 | 分布式状态管理 |
| `communication/base.py` | ~80 | RPC 基类 |
| `communication/client.py` | ~200 | RPC 客户端 |
| `communication/server.py` | ~250 | RPC 服务端 |
| `communication/serialization.py` | ~150 | 序列化工具 |
| `communication/protocol.py` | ~100 | 协议定义 |
| `logging/formatter.py` | ~80 | 日志格式化 |
| `logging/metrics.py` | ~160 | 指标追踪 |
| `random/state.py` | ~240 | RNG 状态管理（含分布式同步） |
| `timing/timer.py` | ~100 | 计时器 |
| `io/video.py` | ~30 | 视频保存 |
| `io/json.py` | ~110 | JSON 序列化 |
| `constants.py` | ~90 | 常量定义 |
| `__init__.py` | ~80 | 导出接口 |
| **总计** | **~2460** | 不含测试 |

---

## 12. 实现优先级

### Phase 1: 核心功能（必须）- 基础设施
1. `device/manager.py` - 设备管理（训练/推理分离）
2. `device/distributed.py` - 分布式设备（NCCL 多机多卡）
3. `constants.py` - 常量定义
4. `timing/timer.py` - 时间测量

### Phase 2: 分布式训练（必须）- CUDA 多卡/多机训练
5. `distributed/launcher.py` - 多进程/多机启动
6. `distributed/reducer.py` - 梯度同步
7. `distributed/state.py` - 分布式状态管理
8. `random/state.py` - RNG 管理（含分布式同步）

### Phase 3: 通信基础设施（必须）- 远程推理基础
9. `communication/base.py` - RPC 基类
10. `communication/client.py` - 客户端基类
11. `communication/server.py` - 服务端基类
12. `communication/serialization.py` - 序列化工具
13. `communication/protocol.py` - 协议定义

### Phase 4: 辅助功能（推荐）
14. `logging/metrics.py` - 指标追踪
15. `logging/formatter.py` - 日志格式化
16. `io/json.py` - JSON 序列化
17. `io/video.py` - 视频保存

---

## 13. 迁移指南

从原始 LeRobot 迁移到 Waleo：

```python
# 原始代码
from lerobot.common.utils.utils import auto_select_torch_device, TimerManager
from lerobot.common.constants import HF_LEROBOT_HOME, ACTION
from lerobot.common.utils.logging_utils import AverageMeter
from lerobot.common.utils.random_utils import set_seed

# 新代码
from waleo.utils.device import get_training_device, TimerManager
from waleo.utils.constants import WALES_HOME, ACTION
from waleo.utils.logging import AverageMeter
from waleo.utils.random import set_seed

# 主要变化：
# 1. 包名从 lerobot 改为 waleo
# 2. auto_select_torch_device 改为 get_training_device/get_inference_device
# 3. HF_LEROBOT_HOME 改为 WALES_HOME
# 4. API 基本兼容
```

---

## 14. 未来扩展

### 14.1 可能的扩展
- 更多通信后端（gRPC, ZeroMQ）
- 更多仿真环境接口（MuJoCo, Brax）
- 性能分析工具集成
- 自动超参数调优支持

### 14.2 版本计划
- v0.1.0: 基础功能实现
- v0.2.0: 通信增强（gRPC 支持）
- v0.3.0: 仿真环境增强（更多后端）

---

## 15. 变更日志

### v0.1.1 (2026-01-08)
- 将仿真环境基类（BaseEnv, VectorEnv, EnvWrapper）移至 M11 仿真环境模块
- 移除 Gym/Gymnasium 依赖
- 优化模块职责划分，M01 专注于基础设施功能

### v0.1.0 (2026-01-08)
- 初始设计
- 从 lerobot 改名为 waleo
- 添加通信基础设施（RPC）
- 支持单机多卡/多机多卡训练
- 支持异步推理架构
