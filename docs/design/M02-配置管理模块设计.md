# M02: 配置管理模块 (waleo-config) 设计文档

> **优先级**: P0 (核心基础)
> **依赖**: M01 (waleo-utils)
> **状态**: 设计中

---

## 1. 模块概述

**模块名称**: `waleo-config`
**版本**: v0.1.0
**描述**: 提供统一的配置管理系统，支持数据类定义、命令行解析、类型验证和插件加载。

---

## 2. 模块职责

### 2.1 核心职责
- 配置数据类定义（Dataset, Eval, Training 等配置）
- 类型系统定义（FeatureType, NormalizationMode）
- 命令行参数解析
- 配置验证和默认值管理
- 配置文件加载（YAML/JSON）
- 插件系统支持

### 2.2 非职责
- 不涉及具体配置值的业务逻辑
- 不涉及配置存储（由调用方决定）
- 不涉及深度学习模型配置（属于 M04 策略模块）

---

## 3. 子模块结构

```
waleo-config/
├── lerobot_config/
│   ├── __init__.py
│   ├── base/            # 基础配置类
│   │   ├── __init__.py
│   │   ├── types.py     # 枚举类型定义
│   │   └── protocol.py  # Protocol 定义
│   ├── dataset/         # 数据集配置
│   │   ├── __init__.py
│   │   └── config.py
│   ├── training/        # 训练配置
│   │   ├── __init__.py
│   │   └── config.py
│   ├── eval/            # 评估配置
│   │   ├── __init__.py
│   │   └── config.py
│   ├── parser/          # 命令行解析
│   │   ├── __init__.py
│   │   ├── cli.py       # CLI 参数解析
│   │   └── plugin.py    # 插件加载
│   └── validation/      # 配置验证
│       ├── __init__.py
│       └── checker.py
├── tests/
│   ├── test_types.py
│   ├── test_dataset_config.py
│   ├── test_training_config.py
│   ├── test_eval_config.py
│   ├── test_parser.py
│   └── test_validation.py
├── examples/
│   ├── minimal_config.py
│   └── full_config.py
├── pyproject.toml
└── README.md
```

---

## 4. API 设计

### 4.1 类型系统 (`base/types.py`)

#### 枚举类型

| 类型名 | 值 | 描述 |
|--------|-----|------|
| `FeatureType` | STATE, VISUAL, ENV, ACTION, REWARD | 特征类型 |
| `NormalizationMode` | MIN_MAX, MEAN_STD, IDENTITY | 归一化模式 |

#### 数据类

| 类名 | 字段 | 描述 |
|------|------|------|
| `PolicyFeature` | type: FeatureType, shape: tuple | 策略特征定义 |

#### 使用示例
```python
from lerobot_config.base import FeatureType, NormalizationMode, PolicyFeature

# 定义特征
obs_feature = PolicyFeature(type=FeatureType.STATE, shape=(10,))
action_feature = PolicyFeature(type=FeatureType.ACTION, shape=(6,))

# 指定归一化模式
norm_mode = NormalizationMode.MEAN_STD
```

#### 依赖
- `dataclasses` (标准库)
- `enum` (标准库)
- `typing` (标准库)

---

### 4.2 数据集配置 (`dataset/config.py`)

#### 数据类定义

```python
@dataclass
class ImageTransformsConfig:
    """图像变换配置"""
    enable: bool = True
    random_resize: bool = False
    random_crop: bool = False
    horizontal_flip: bool = False
    vertical_flip: bool = False

@dataclass
class DatasetConfig:
    """数据集配置"""
    # 必需参数
    repo_id: str

    # 可选参数
    root: str | None = None           # 数据集存储根目录
    episodes: list[int] | None = None # 指定使用的 episode
    image_transforms: ImageTransformsConfig = field(default_factory=ImageTransformsConfig)
    revision: str | None = None       # Hub 版本
    use_imagenet_stats: bool = True   # 使用 ImageNet 统计信息
    video_backend: str = "pyav"       # 视频后端: "pyav" 或 "video_codec"

    def __post_init__(self):
        """配置验证"""
        if self.video_backend not in ["pyav", "video_codec"]:
            raise ValueError(f"Invalid video_backend: {self.video_backend}")
```

#### 配置示例

```yaml
# dataset_config.yaml
repo_id: "waleo/pusht"
root: "data/datasets"
episodes: [1, 2, 3, 4, 5]
image_transforms:
  enable: true
  random_crop: true
use_imagenet_stats: true
video_backend: "pyav"
```

#### 使用示例
```python
from lerobot_config.dataset import DatasetConfig

# 方式1: 直接创建
config = DatasetConfig(
    repo_id="waleo/pusht",
    episodes=[1, 2, 3, 4, 5],
    use_imagenet_stats=False
)

# 方式2: 从文件加载
config = DatasetConfig.from_yaml("dataset_config.yaml")

# 方式3: 命令行覆盖
# python train.py --dataset.repo_id=waleo/aloha --dataset.episodes=[1,2,3]
```

#### 依赖
- `dataclasses` (标准库)
- `yaml` (可选，用于 YAML 文件加载)
- `lerobot-utils.base` (M01，用于类型提示)

---

### 4.3 训练配置 (`training/config.py`)

#### 数据类定义

```python
@dataclass
class OptimizerConfig:
    """优化器配置"""
    type: str = "adam"          # adam, adamw, sgd
    lr: float = 1e-4
    betas: tuple[float, float] = (0.9, 0.999)
    eps: float = 1e-8
    weight_decay: float = 0.0

@dataclass
class SchedulerConfig:
    """学习率调度器配置"""
    type: str = "cosine"        # cosine, step, constant
    num_warmup_steps: int = 500
    min_lr_ratio: float = 0.0

@dataclass
class CheckpointConfig:
    """检查点配置"""
    save_every: int = 5000      # 每 N 步保存
    save_total_limit: int = 3   # 最多保留 N 个检查点
    save_optimizer: bool = True

@dataclass
class TrainingConfig:
    """训练配置"""
    # 数据集相关
    offline_steps: int = 50000
    online_steps: int = 0
    rollout_n_episodes: int = 50
    rollout_batch_size: int = 8

    # 训练超参数
    batch_size: int = 64
    num_workers: int = 4
    seed: int = 1337

    # 设备和日志
    device: str = "cuda"        # cuda, mps, cpu
    log_freq: int = 100
    eval_freq: int = 5000

    # 子配置
    optimizer: OptimizerConfig = field(default_factory=OptimizerConfig)
    scheduler: SchedulerConfig = field(default_factory=SchedulerConfig)
    checkpoint: CheckpointConfig = field(default_factory=CheckpointConfig)

    def __post_init__(self):
        """配置验证"""
        if self.offline_steps <= 0:
            raise ValueError("offline_steps must be positive")
        if self.batch_size <= 0:
            raise ValueError("batch_size must be positive")
```

#### 配置示例

```yaml
# training_config.yaml
offline_steps: 100000
batch_size: 32
num_workers: 8
device: "cuda"
optimizer:
  type: "adamw"
  lr: 0.0001
  weight_decay: 0.0001
scheduler:
  type: "cosine"
  num_warmup_steps: 1000
checkpoint:
  save_every: 10000
  save_total_limit: 5
```

#### 使用示例
```python
from lerobot_config.training import TrainingConfig

config = TrainingConfig(
    offline_steps=100000,
    batch_size=32,
    optimizer=OptimizerConfig(type="adamw", lr=1e-4, weight_decay=1e-4)
)

# 访问嵌套配置
lr = config.optimizer.lr
```

#### 依赖
- `dataclasses` (标准库)
- `lerobot-utils.device` (M01，用于设备验证)

---

### 4.4 评估配置 (`eval/config.py`)

#### 数据类定义

```python
@dataclass
class EvalConfig:
    """评估配置"""
    n_episodes: int = 50
    batch_size: int = 50          # 并行环境数量
    use_async_envs: bool = False  # 使用异步环境（多进程）
    save_video: bool = True       # 保存评估视频
    video_fps: int = 30

    def __post_init__(self):
        """配置验证"""
        if self.batch_size > self.n_episodes:
            import warnings
            warnings.warn(
                f"Eval batch size ({self.batch_size}) > n_episodes ({self.n_episodes}). "
                "Some environments will be unused."
            )
```

#### 使用示例
```python
from lerobot_config.eval import EvalConfig

config = EvalConfig(
    n_episodes=100,
    batch_size=10,
    save_video=True
)
```

#### 依赖
- `dataclasses` (标准库)
- `warnings` (标准库)

---

### 4.5 命令行解析 (`parser/cli.py`)

#### 函数列表

| 函数名 | 输入 | 输出 | 描述 |
|--------|------|------|------|
| `parse_config(config_cls, args=None)` | `type, List[str]` | `config_cls` | 解析命令行并创建配置对象 |
| `parse_arg(arg_name, args=None)` | `str, List[str]` | `str | None` | 解析单个参数 |
| `get_cli_overrides(field_name, args=None)` | `str, List[str]` | `List[str]` | 获取字段的所有覆盖参数 |
| `filter_args(fields_to_filter, args=None)` | `str | List[str], List[str]` | `List[str]` | 过滤掉指定字段的参数 |

#### 使用示例

```python
from lerobot_config.parser import parse_config
from lerobot_config.training import TrainingConfig

# 假设命令行: python train.py --batch_size=128 --optimizer.lr=0.001
config = parse_config(TrainingConfig)
# 结果: TrainingConfig(batch_size=128, optimizer=OptimizerConfig(lr=0.001, ...))

# 从文件 + 命令行覆盖
config = parse_config(
    TrainingConfig,
    config_path="training_config.yaml",
    args=["--batch_size=256"]
)
# 结果: 使用文件中的默认值，但 batch_size 被覆盖为 256
```

#### CLI 参数格式

```
# 顶级参数
--offline_steps=100000

# 嵌套参数（使用点号）
--optimizer.lr=0.001
--optimizer.weight_decay=0.0001

# 列表参数
--episodes=[1,2,3,4,5]

# 布尔参数
--use_async_envs=true
--save_video=false
```

#### 依赖
- `argparse` (标准库)
- `sys` (标准库)
- `lerobot-utils` (M01，用于工具函数)

---

### 4.6 插件系统 (`parser/plugin.py`)

#### 函数列表

| 函数名 | 输入 | 输出 | 描述 |
|--------|------|------|------|
| `load_plugin(plugin_path)` | `str` | 无 | 加载插件包 |
| `parse_plugin_args(suffix, args)` | `str, List[str]` | `dict` | 解析插件相关参数 |

#### 使用示例

```python
from lerobot_config.parser import load_plugin, parse_plugin_args

# 加载自定义插件
load_plugin("my_company.custom_plugins")

# 插件会在导入时注册自己的配置类
# 例如: 注册 CustomPolicyConfig
```

#### 插件开发

```python
# my_company/custom_plugins/policy.py
from lerobot_config.base import PolicyConfig
from lerobot_config.parser import register_config

@register_config  # 装饰器自动注册
class CustomPolicyConfig(PolicyConfig):
    hidden_dim: int = 512
    num_layers: int = 4
```

#### 依赖
- `importlib` (标准库)
- `pkgutil` (标准库)

---

### 4.7 配置验证 (`validation/checker.py`)

#### 函数列表

| 函数名 | 输入 | 输出 | 描述 |
|--------|------|------|------|
| `validate_config(config)` | `Any` | `bool` | 验证配置对象 |
| `check_config_consistency(configs)` | `dict` | `List[str]` | 检查多个配置的一致性 |

#### 使用示例

```python
from lerobot_config.validation import validate_config, check_config_consistency
from lerobot_config.training import TrainingConfig
from lerobot_config.dataset import DatasetConfig

# 验证单个配置
train_cfg = TrainingConfig(batch_size=32)
is_valid = validate_config(train_cfg)

# 检查配置间一致性
dataset_cfg = DatasetConfig(repo_id="waleo/pusht")
issues = check_config_consistency({
    "training": train_cfg,
    "dataset": dataset_cfg
})
# 例如: 检查 batch_size 是否与 num_workers 兼容
```

#### 依赖
- `lerobot-utils` (M01，用于日志)

---

## 5. 模块接口

### 5.1 公开 API (`__init__.py`)

```python
# 基础类型
from lerobot_config.base import (
    FeatureType,
    NormalizationMode,
    PolicyFeature,
    DictLike,
)

# 数据集配置
from lerobot_config.dataset import (
    DatasetConfig,
    ImageTransformsConfig,
)

# 训练配置
from lerobot_config.training import (
    TrainingConfig,
    OptimizerConfig,
    SchedulerConfig,
    CheckpointConfig,
)

# 评估配置
from lerobot_config.eval import (
    EvalConfig,
)

# 解析器
from lerobot_config.parser import (
    parse_config,
    parse_arg,
    get_cli_overrides,
    load_plugin,
)

# 验证
from lerobot_config.validation import (
    validate_config,
    check_config_consistency,
)
```

### 5.2 版本信息

```python
__version__ = "0.1.0"
```

---

## 6. 与其他模块的接口

### 6.1 依赖关系

| 依赖模块 | 使用的功能 | 用途 |
|----------|------------|------|
| M01 工具模块 | `get_device`, `init_logging`, `get_logger` | 设备验证和日志 |

### 6.2 被依赖关系

| 依赖模块 | 使用的配置 | 用途 |
|----------|------------|------|
| M03 数据集模块 | `DatasetConfig` | 数据加载参数 |
| M04 策略模块 | `PolicyConfig` (基础) | 策略配置 |
| M11 环境模块 | `EvalConfig` | 评估环境配置 |
| M13 训练脚本 | `TrainingConfig`, `CheckpointConfig`, `OptimizerConfig`, `SchedulerConfig` | 训练流程控制 |

### 6.3 数据流

```
┌─────────────────────────────────────────────────────────────┐
│                   配置输入来源                               │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ YAML文件 │  │ JSON文件 │  │ CLI参数  │  │  默认值  │   │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘   │
│       └─────────────┴─────────────┴─────────────┘         │
│                           │                                 │
│                           ▼                                 │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              M02: waleo-config                      │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │  │
│  │  │  解析器     │  │  验证器     │  │  类型系统   │  │  │
│  │  └─────────────┘  └─────────────┘  └─────────────┘  │  │
│  └──────────────────────────────────────────────────────┘  │
│                           │                                 │
│                           ▼                                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │   M03    │  │   M04    │  │   M13    │   │
│  └──────────┘  └──────────┘  └──────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 7. 测试策略

### 7.1 单元测试

```python
# tests/test_types.py
def test_feature_type_enum():
    assert FeatureType.STATE == "STATE"
    assert FeatureType.VISUAL == "VISUAL"

# tests/test_parser.py
def test_parse_config():
    config = parse_config(
        TrainingConfig,
        args=["--batch_size=128", "--optimizer.lr=0.001"]
    )
    assert config.batch_size == 128
    assert config.optimizer.lr == 0.001

def test_parse_nested_config():
    config = parse_config(
        TrainingConfig,
        args=["--optimizer.type=adamw"]
    )
    assert config.optimizer.type == "adamw"

# tests/test_validation.py
def test_config_validation():
    config = TrainingConfig(batch_size=-1)
    assert not validate_config(config)
```

### 7.2 测试覆盖率目标
- 核心功能：> 90%
- 边界情况：> 85%
- 总体：> 85%

---

## 8. 性能考虑

### 8.1 性能目标
- `parse_config()`: < 50ms（不含文件 I/O）
- `validate_config()`: < 10ms
- `load_plugin()`: < 1s（取决于插件大小）

### 8.2 优化策略
- 配置解析使用惰性求值
- 避免重复验证
- 缓存插件元数据

---

## 9. 依赖项

### 9.1 核心依赖（必需）
```toml
[tool.poetry.dependencies]
python = "^3.9"
lerobot-utils = { path = "../lerobot-utils", develop = true }
draccus = "^0.9.0"
pyyaml = { version = "^6.0", optional = true }
```

### 9.2 开发依赖
```toml
[tool.poetry.dev-dependencies]
pytest = "^7.4.0"
pytest-cov = "^4.1.0"
black = "^23.0.0"
mypy = "^1.5.0"
```

### 9.3 可选依赖
- `pyyaml`: YAML 文件支持
- `toml`: TOML 文件支持

---

## 10. 兼容性

### 10.1 Python 版本
- 最低：Python 3.9
- 测试：3.9, 3.10, 3.11

### 10.2 平台支持
- Linux: ✅ 完全支持
- macOS: ✅ 完全支持
- Windows: ✅ 完全支持

---

## 11. 文件大小估算

| 文件/目录 | 估计行数 | 说明 |
|-----------|----------|------|
| `base/types.py` | ~50 | 类型定义 |
| `dataset/config.py` | ~80 | 数据集配置 |
| `training/config.py` | ~150 | 训练配置 |
| `eval/config.py` | ~40 | 评估配置 |
| `parser/cli.py` | ~200 | CLI 解析器 |
| `parser/plugin.py` | ~100 | 插件系统 |
| `validation/checker.py` | ~120 | 配置验证 |
| `__init__.py` | ~60 | 导出接口 |
| **总计** | **~800** | 不含测试 |

---

## 12. 实现优先级

### Phase 1: 核心类型和配置（必须）
1. `base/types.py` - 类型系统
2. `dataset/config.py` - 数据集配置
3. `eval/config.py` - 评估配置

### Phase 2: 解析和验证（必须）
4. `parser/cli.py` - CLI 解析器
5. `validation/checker.py` - 配置验证

### Phase 3: 高级功能（推荐）
6. `training/config.py` - 训练配置
7. `parser/plugin.py` - 插件系统

---

## 13. 迁移指南

从原始 LeRobot 迁移到新模块：

```python
# 原始代码
from lerobot.configs import parse_arg
from lerobot.configs.default import DatasetConfig, EvalConfig

# 新代码
from lerobot_config.parser import parse_config, parse_arg
from lerobot_config.dataset import DatasetConfig
from lerobot_config.eval import EvalConfig

# 使用 parse_config 替代 draccus.parse
config = parse_config(DatasetConfig, args=sys.argv[1:])
```

---

## 14. 未来扩展

### 14.1 可能的扩展
- JSON Schema 生成
- 配置文档自动生成
- 配置可视化工具
- 更多文件格式支持（TOML, INI）

### 14.2 版本计划
- v0.1.0: 基础功能实现
- v0.2.0: 插件系统增强
- v0.3.0: 配置文档生成

---

## 15. 变更日志

### v0.1.0 (2026-01-08)
- 初始设计
- 定义核心配置类
- 确定解析器架构
