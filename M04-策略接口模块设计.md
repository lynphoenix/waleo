# M04: 策略接口模块 (waleo-policy-base) 设计文档

> **优先级**: P0 (策略核心)
> **依赖**: M01 (waleo-utils), M02 (waleo-config)
> **状态**: 设计中

---

## 1. 模块概述

**模块名称**: `waleo-policy-base`
**版本**: v0.1.0
**描述**: 提供机器人学习策略的统一接口抽象，包括策略基类（BasePolicy）、归一化工具、策略工厂和配置系统。所有具体策略实现（ACT、Diffusion 等）都必须继承此模块提供的基类。

---

## 2. 模块职责

### 2.1 核心职责

- **策略基类（BasePolicy）**：定义所有策略必须实现的抽象接口
- **归一化工具**：提供输入/输出的归一化和反归一化
- **策略工厂**：注册和创建策略实例
- **基础配置类**：定义策略配置的数据结构
- **特征类型系统**：定义观察和动作的特征类型
- **策略评估指标**：提供通用的评估指标计算

### 2.2 非职责

- 不涉及具体策略模型实现（属于 M05 ACT、M06 Diffusion 等）
- 不涉及训练循环逻辑（属于 M13 训练模块）
- 不涉及数据加载（属于 M03 数据集模块）

---

## 3. 子模块结构

```
waleo-policy-base/
├── waleo_policy/
│   ├── __init__.py
│   ├── base/                # 策略基类
│   │   ├── __init__.py
│   │   ├── policy.py         # BasePolicy 抽象基类
│   │   ├── config.py         # PolicyConfig 配置基类
│   │   └── types.py          # 数据类型定义
│   ├── normalize/            # 归一化工具
│   │   ├── __init__.py
│   │   ├── normalize.py      # Normalize 输入归一化
│   │   └── unnormalize.py    # Unnormalize 输出反归一化
│   ├── factory/              # 策略工厂
│   │   ├── __init__.py
│   │   └── factory.py        # PolicyFactory 工厂类
│   └── utils/                # 工具函数
│       ├── __init__.py
│       └── metrics.py        # 策略评估指标
├── tests/
│   ├── test_base_policy.py
│   ├── test_normalize.py
│   ├── test_factory.py
│   └── test_metrics.py
├── examples/
│   ├── custom_policy.py      # 自定义策略示例
│   └── use_factory.py        # 工厂使用示例
├── pyproject.toml
└── README.md
```

---

## 4. API 设计

### 4.1 数据类型定义 (`base/types.py`)

#### 4.1.1 特征类型枚举

```python
from enum import Enum

class FeatureType(Enum):
    """特征类型"""
    STATE = "state"              # 机器人本体状态（关节位置、速度等）
    VISUAL = "visual"            # 图像观测
    ENV = "env_state"            # 环境状态（物体位置等）
    ACTION = "action"            # 动作
```

#### 4.1.2 归一化模式枚举

```python
class NormalizationMode(Enum):
    """归一化模式"""
    MEAN_STD = "MEAN_STD"        # (x - mean) / std
    MIN_MAX = "MIN_MAX"          # 映射到 [-1, 1]
    IDENTITY = "IDENTITY"        # 不做归一化
```

#### 4.1.3 策略特征数据类

```python
from dataclasses import dataclass
from typing import Tuple

@dataclass
class PolicyFeature:
    """策略特征定义"""
    type: FeatureType            # 特征类型
    shape: Tuple[int, ...]       # 特征形状
    name: str | None = None      # 特征名称（可选）
```

#### 使用示例

```python
from waleo_policy.base import FeatureType, NormalizationMode, PolicyFeature

# 定义特征
obs_feature = PolicyFeature(type=FeatureType.STATE, shape=(10,), name="joint_position")
action_feature = PolicyFeature(type=FeatureType.ACTION, shape=(6,))

# 指定归一化模式
norm_mode = NormalizationMode.MEAN_STD
```

#### 依赖

- `enum` (标准库)
- `dataclasses` (标准库)
- `typing` (标准库)

---

### 4.2 策略配置基类 (`base/config.py`)

#### 4.2.1 PolicyConfig 类

```python
from dataclasses import dataclass, field
from typing import Any

@dataclass
class PolicyConfig:
    """策略配置基类

    所有具体策略的配置类都应该继承此类
    """
    # 基础配置
    name: str = "base_policy"                # 策略名称
    input_shapes: dict[str, tuple] = field(default_factory=dict)   # 输入形状
    output_shapes: dict[str, tuple] = field(default_factory=dict)  # 输出形状

    # 归一化配置
    normalization_mapping: dict[str, NormalizationMode] = field(
        default_factory=lambda: {
            "observation.state": NormalizationMode.MEAN_STD,
            "observation.image": NormalizationMode.IDENTITY,
            "action": NormalizationMode.MIN_MAX,
        }
    )

    # 设备配置
    device: str = "cuda"                      # 设备 (cuda/mps/cpu)

    # 其他配置
    kwargs: dict[str, Any] = field(default_factory=dict)  # 其他参数

    def __post_init__(self):
        """配置验证"""
        if self.device not in ["cuda", "mps", "cpu"]:
            raise ValueError(f"Invalid device: {self.device}")
```

#### 使用示例

```python
from waleo_policy.base import PolicyConfig, NormalizationMode

# 创建配置
config = PolicyConfig(
    name="my_policy",
    input_shapes={"observation.state": (10,), "observation.image": (3, 224, 224)},
    output_shapes={"action": (6,)},
    normalization_mapping={
        "observation.state": NormalizationMode.MEAN_STD,
        "observation.image": NormalizationMode.IDENTITY,
        "action": NormalizationMode.MIN_MAX,
    },
    device="cuda"
)
```

#### 依赖

- `dataclasses` (标准库)
- `waleo_policy.base.types` (M04)

---

### 4.3 策略基类 (`base/policy.py`)

#### 4.3.1 BasePolicy 抽象类

```python
import torch
import torch.nn as nn
from abc import ABC, abstractmethod
from typing import Any
from pathlib import Path

class BasePolicy(nn.Module, ABC):
    """策略基类

    所有具体策略实现（ACT, Diffusion 等）必须继承此类并实现所有抽象方法

    子类必须定义：
    - config_class: 配置类
    - name: 策略名称
    """

    # 子类必须定义
    config_class: type[PolicyConfig]
    name: str

    def __init__(self, config: PolicyConfig):
        """
        Args:
            config: 策略配置
        """
        super().__init__()
        self.config = config
        self.device = torch.device(config.device)

    @abstractmethod
    def get_optim_params(self) -> list[dict]:
        """返回优化器参数组

        用于创建优化器，支持不同参数组使用不同学习率

        Returns:
            参数组列表，例如：
            [
                {"params": model.encoder.parameters(), "lr": 1e-4},
                {"params": model.decoder.parameters(), "lr": 1e-5}
            ]
        """
        pass

    @abstractmethod
    def reset(self):
        """重置策略状态

        在每个 episode 开始时调用，用于清除缓存、重置隐藏状态等
        """
        pass

    @abstractmethod
    def forward(self, batch: dict) -> tuple[torch.Tensor, dict]:
        """训练前向传播

        计算损失并返回日志信息

        Args:
            batch: 批量数据，包含：
                - observation: 观察数据
                - action: 动作数据
                - 其他训练所需数据

        Returns:
            loss: 损失张量
            logs: 日志字典，例如 {"loss": 0.5, "lr": 0.001}
        """
        pass

    @abstractmethod
    def select_action(self, batch: dict) -> torch.Tensor:
        """选择动作（推理）

        Args:
            batch: 批量数据，包含 observation

        Returns:
            action: 动作张量，形状 (batch_size, action_dim)
        """
        pass

    def save(self, path: Path | str):
        """保存策略

        Args:
            path: 保存路径
        """
        path = Path(path)
        path.mkdir(parents=True, exist_ok=True)

        # 保存模型
        torch.save({
            "model_state_dict": self.state_dict(),
            "config": self.config,
        }, path / "policy.safetensors")

    @classmethod
    def load(cls, path: Path | str):
        """加载策略

        Args:
            path: 加载路径

        Returns:
            策略实例
        """
        path = Path(path)
        checkpoint = torch.load(path / "policy.safetensors")

        # 创建实例
        config = checkpoint["config"]
        policy = cls(config)

        # 加载权重
        policy.load_state_dict(checkpoint["model_state_dict"])
        policy.eval()

        return policy

    def eval_action(self, batch: dict) -> dict:
        """评估动作（包含元数据）

        Args:
            batch: 批量数据

        Returns:
            包含 action 和其他信息的字典
        """
        with torch.no_grad():
            action = self.select_action(batch)

        return {
            "action": action.cpu().numpy(),
            "success": True,
        }
```

#### 使用示例

```python
import torch
import torch.nn as nn
from waleo_policy.base import BasePolicy, PolicyConfig

class MyPolicy(BasePolicy):
    """自定义策略示例"""

    name = "my_policy"
    config_class = PolicyConfig

    def __init__(self, config: PolicyConfig):
        super().__init__(config)
        # 构建网络
        self.network = nn.Sequential(
            nn.Linear(10, 64),
            nn.ReLU(),
            nn.Linear(64, 6)
        )

    def get_optim_params(self):
        return [{"params": self.parameters(), "lr": 1e-4}]

    def reset(self):
        # 清除缓存等
        pass

    def forward(self, batch):
        obs = batch["observation.state"]
        action = batch["action"]

        pred_action = self.network(obs)
        loss = nn.functional.mse_loss(pred_action, action)

        return loss, {"loss": loss.item()}

    def select_action(self, batch):
        obs = batch["observation.state"]
        return self.network(obs)

# 使用
config = PolicyConfig(
    name="my_policy",
    input_shapes={"observation.state": (10,)},
    output_shapes={"action": (6,)}
)
policy = MyPolicy(config)

# 训练
batch = {
    "observation.state": torch.randn(32, 10),
    "action": torch.randn(32, 6)
}
loss, logs = policy(batch)
loss.backward()

# 推理
policy.reset()
action = policy.select_action({"observation.state": torch.randn(1, 10)})
```

#### 依赖

- `torch` (PyTorch)
- `abc` (标准库)
- `waleo_policy.base.config` (M04)
- `waleo_utils.device` (M01) - get_training_device

---

### 4.4 归一化工具 (`normalize/`)

#### 4.4.1 Normalize 类 (`normalize/normalize.py`)

```python
import torch
import torch.nn as nn
from typing import dict

class Normalize(nn.Module):
    """输入归一化

    对观察数据进行归一化处理
    """

    def __init__(
        self,
        input_shapes: dict[str, tuple],
        normalization_mapping: dict[str, NormalizationMode],
        stats: dict[str, dict] | None = None
    ):
        """
        Args:
            input_shapes: 输入形状映射
            normalization_mapping: 归一化模式映射
            stats: 统计信息，格式：
                {
                    "observation.state": {
                        "mean": np.array([...]),
                        "std": np.array([...])
                    }
                }
        """
        super().__init__()
        self.input_shapes = input_shapes
        self.normalization_mapping = normalization_mapping

        # 注册统计信息为 buffer
        self.register_buffers("means", {})
        self.register_buffers("stds", {})

        if stats is not None:
            for key, stat in stats.items():
                if "mean" in stat:
                    self.means[key] = torch.from_numpy(stat["mean"]).float()
                if "std" in stat:
                    self.stds[key] = torch.from_numpy(stat["std"]).float()

    def forward(self, batch: dict) -> dict:
        """对 batch 进行归一化

        Args:
            batch: 输入 batch

        Returns:
            归一化后的 batch
        """
        normalized_batch = {}

        for key, value in batch.items():
            if key in self.normalization_mapping:
                mode = self.normalization_mapping[key]

                if mode == NormalizationMode.MEAN_STD:
                    # (x - mean) / std
                    mean = self.means.get(key, 0)
                    std = self.stds.get(key, 1)
                    value = (value - mean) / (std + 1e-8)

                elif mode == NormalizationMode.MIN_MAX:
                    # 映射到 [-1, 1]
                    # 需要从 stats 中获取 min/max
                    # 这里简化处理，假设数据已标准化
                    value = torch.tanh(value)

                # IDENTITY: 不做处理

            normalized_batch[key] = value

        return normalized_batch
```

#### 4.4.2 Unnormalize 类 (`normalize/unnormalize.py`)

```python
import torch
import torch.nn as nn

class Unnormalize(nn.Module):
    """输出反归一化

    将模型输出反归一化到原始动作空间
    """

    def __init__(
        self,
        output_shapes: dict[str, tuple],
        normalization_mapping: dict[str, NormalizationMode],
        stats: dict[str, dict] | None = None
    ):
        """
        Args:
            output_shapes: 输出形状映射
            normalization_mapping: 归一化模式映射
            stats: 统计信息
        """
        super().__init__()
        self.output_shapes = output_shapes
        self.normalization_mapping = normalization_mapping

        # 注册统计信息为 buffer
        self.register_buffers("means", {})
        self.register_buffers("stds", {})

        if stats is not None:
            for key, stat in stats.items():
                if "mean" in stat:
                    self.means[key] = torch.from_numpy(stat["mean"]).float()
                if "std" in stat:
                    self.stds[key] = torch.from_numpy(stat["std"]).float()

    def forward(self, batch: dict) -> dict:
        """对 batch 进行反归一化

        Args:
            batch: 模型输出 batch

        Returns:
            反归一化后的 batch
        """
        unnormalized_batch = {}

        for key, value in batch.items():
            if key in self.normalization_mapping:
                mode = self.normalization_mapping[key]

                if mode == NormalizationMode.MEAN_STD:
                    # x * std + mean
                    mean = self.means.get(key, 0)
                    std = self.stds.get(key, 1)
                    value = value * (std + 1e-8) + mean

                elif mode == NormalizationMode.MIN_MAX:
                    # 从 [-1, 1] 映射回原始范围
                    # 简化处理
                    value = torch.atanh(torch.clamp(value, -0.99, 0.99))

                # IDENTITY: 不做处理

            unnormalized_batch[key] = value

        return unnormalized_batch
```

#### 使用示例

```python
from waleo_policy.normalize import Normalize, Unnormalize
from waleo_policy.base import NormalizationMode

# 统计信息
stats = {
    "observation.state": {
        "mean": np.array([0.5, 0.3, 0.2]),
        "std": np.array([1.0, 0.8, 0.6])
    },
    "action": {
        "mean": np.array([0.0, 0.0]),
        "std": np.array([1.0, 1.0])
    }
}

# 归一化映射
norm_mapping = {
    "observation.state": NormalizationMode.MEAN_STD,
    "action": NormalizationMode.MEAN_STD
}

# 创建归一化/反归一化
normalize = Normalize(
    input_shapes={"observation.state": (3,), "action": (2,)},
    normalization_mapping=norm_mapping,
    stats=stats
)

unnormalize = Unnormalize(
    output_shapes={"action": (2,)},
    normalization_mapping={"action": NormalizationMode.MEAN_STD},
    stats=stats
)

# 使用
batch = {
    "observation.state": torch.tensor([[0.5, 0.3, 0.2]]),
    "action": torch.tensor([[0.0, 0.0]])
}

# 归一化
norm_batch = normalize(batch)

# 模型推理...
# output = model(norm_batch)

# 反归一化
# unnorm_output = unnormalize({"action": output})
```

#### 依赖

- `torch` (PyTorch)
- `numpy` (数组操作)
- `waleo_policy.base.types` (M04)

---

### 4.5 策略工厂 (`factory/factory.py`)

#### 4.5.1 PolicyFactory 类

```python
from typing import type, dict

class PolicyFactory:
    """策略工厂

    用于注册和创建策略实例
    """

    # 注册表：策略名称 -> 策略类
    _registry: dict[str, type[BasePolicy]] = {}

    @staticmethod
    def register(name: str, cls: type[BasePolicy]):
        """注册策略类型

        Args:
            name: 策略名称
            cls: 策略类
        """
        if not issubclass(cls, BasePolicy):
            raise TypeError(f"{cls} must be a subclass of BasePolicy")

        PolicyFactory._registry[name] = cls

    @staticmethod
    def create(name: str, config: PolicyConfig, **kwargs) -> BasePolicy:
        """创建策略实例

        Args:
            name: 策略名称
            config: 策略配置
            **kwargs: 额外参数

        Returns:
            策略实例

        Raises:
            ValueError: 如果策略名称未注册
        """
        if name not in PolicyFactory._registry:
            raise ValueError(f"Unknown policy: {name}. Available: {list(PolicyFactory._registry.keys())}")

        policy_cls = PolicyFactory._registry[name]
        return policy_cls(config, **kwargs)

    @staticmethod
    def list_policies() -> list[str]:
        """列出所有已注册的策略

        Returns:
            策略名称列表
        """
        return list(PolicyFactory._registry.keys())

    @staticmethod
    def is_registered(name: str) -> bool:
        """检查策略是否已注册

        Args:
            name: 策略名称

        Returns:
            是否已注册
        """
        return name in PolicyFactory._registry
```

#### 使用示例

```python
from waleo_policy.factory import PolicyFactory
from waleo_policy.base import BasePolicy, PolicyConfig

# 注册策略
PolicyFactory.register("my_policy", MyPolicy)

# 从工厂创建
config = PolicyConfig(name="my_policy")
policy = PolicyFactory.create("my_policy", config)

# 列出所有策略
print(PolicyFactory.list_policies())  # ['my_policy']
```

#### 依赖

- `waleo_policy.base.policy` (M04)

---

### 4.6 评估指标 (`utils/metrics.py`)

#### 4.6.1 指标计算函数

```python
import numpy as np
from typing import dict

def compute_success_rate(episode_returns: list[float], success_threshold: float = 0.0) -> float:
    """计算成功率

    Args:
        episode_returns: 每个 episode 的回报
        success_threshold: 成功阈值

    Returns:
        成功率 (0-1)
    """
    successes = [r > success_threshold for r in episode_returns]
    return np.mean(successes)

def compute_average_return(episode_returns: list[float]) -> float:
    """计算平均回报

    Args:
        episode_returns: 每个 episode 的回报

    Returns:
        平均回报
    """
    return np.mean(episode_returns)

def compute_std_return(episode_returns: list[float]) -> float:
    """计算回报标准差

    Args:
        episode_returns: 每个 episode 的回报

    Returns:
        回报标准差
    """
    return np.std(episode_returns)

def compute_metrics(episode_returns: list[float], success_threshold: float = 0.0) -> dict:
    """计算所有评估指标

    Args:
        episode_returns: 每个 episode 的回报
        success_threshold: 成功阈值

    Returns:
        指标字典
    """
    return {
        "success_rate": compute_success_rate(episode_returns, success_threshold),
        "average_return": compute_average_return(episode_returns),
        "std_return": compute_std_return(episode_returns),
        "max_return": np.max(episode_returns),
        "min_return": np.min(episode_returns),
        "num_episodes": len(episode_returns),
    }
```

#### 使用示例

```python
from waleo_policy.utils import compute_metrics

episode_returns = [1.0, 0.8, 1.0, 0.0, 0.9]
metrics = compute_metrics(episode_returns, success_threshold=0.5)

print(metrics)
# {
#     'success_rate': 0.8,
#     'average_return': 0.74,
#     'std_return': 0.37,
#     'max_return': 1.0,
#     'min_return': 0.0,
#     'num_episodes': 5
# }
```

#### 依赖

- `numpy` (数组操作)

---

## 5. 模块接口

### 5.1 公开 API (`__init__.py`)

```python
# 策略基类
from waleo_policy.base import (
    BasePolicy,
    PolicyConfig,
    FeatureType,
    NormalizationMode,
    PolicyFeature,
)

# 归一化
from waleo_policy.normalize import (
    Normalize,
    Unnormalize,
)

# 工厂
from waleo_policy.factory import (
    PolicyFactory,
)

# 工具
from waleo_policy.utils import (
    compute_success_rate,
    compute_average_return,
    compute_std_return,
    compute_metrics,
)
```

### 5.2 版本信息

```python
__version__ = "0.1.0"
```

---

## 6. 与其他模块的接口

### 6.1 依赖关系

| 依赖模块 | 使用的功能 | 用途 |
|----------|------------|------|
| M01 工具模块 | `get_training_device`, `TimerManager` | 设备选择、性能测量 |
| M02 配置模块 | 配置系统 | 策略配置管理 |

### 6.2 被依赖关系

| 依赖模块 | 使用的功能 | 用途 |
|----------|------------|------|
| M05 ACT 策略 | 继承 `BasePolicy` | ACT 策略实现 |
| M06 Diffusion 策略 | 继承 `BasePolicy` | Diffusion 策略实现 |
| M13 训练脚本 | `PolicyFactory` | 创建策略实例 |
| M18 评估脚本 | `compute_metrics` | 评估策略性能 |

### 6.3 数据流

```
┌─────────────────────────────────────────────────────────────┐
│                    训练/推理流程                             │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│  ┌──────────────────────────────────────────────────────┐  │
│  │  M04: waleo-policy-base                             │  │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐    │  │
│  │  │ BasePolicy │  │ Normalize  │  │  Factory   │    │  │
│  │  └────────────┘  └────────────┘  └────────────┘    │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                      具体策略实现                             │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐            │
│  │    ACT     │  │ Diffusion  │  │   Others   │            │
│  └────────────┘  └────────────┘  └────────────┘            │
└─────────────────────────────────────────────────────────────┘
```

---

## 7. 使用场景

### 7.1 实现自定义策略

```python
import torch
import torch.nn as nn
from waleo_policy.base import BasePolicy, PolicyConfig, NormalizationMode
from waleo_policy.factory import PolicyFactory

class SimplePolicy(BasePolicy):
    """简单的 MLP 策略"""

    name = "simple"
    config_class = PolicyConfig

    def __init__(self, config: PolicyConfig):
        super().__init__(config)

        # 获取输入/输出维度
        obs_dim = config.input_shapes["observation.state"][0]
        action_dim = config.output_shapes["action"][0]

        # 构建网络
        self.network = nn.Sequential(
            nn.Linear(obs_dim, 256),
            nn.ReLU(),
            nn.Linear(256, 256),
            nn.ReLU(),
            nn.Linear(256, action_dim)
        )

    def get_optim_params(self):
        return [{"params": self.parameters(), "lr": 1e-4}]

    def reset(self):
        pass

    def forward(self, batch):
        obs = batch["observation.state"]
        action = batch["action"]

        pred_action = self.network(obs)
        loss = nn.functional.mse_loss(pred_action, action)

        return loss, {"loss": loss.item()}

    def select_action(self, batch):
        obs = batch["observation.state"]
        return self.network(obs)

# 注册策略
PolicyFactory.register("simple", SimplePolicy)

# 使用
config = PolicyConfig(
    name="simple",
    input_shapes={"observation.state": (10,)},
    output_shapes={"action": (6,)}
)
policy = PolicyFactory.create("simple", config)
```

### 7.2 使用归一化

```python
from waleo_policy.normalize import Normalize
from waleo_policy.base import NormalizationMode

# 统计信息（通常从数据集计算得到）
stats = {
    "observation.state": {
        "mean": np.zeros(10),
        "std": np.ones(10)
    }
}

# 创建归一化层
normalize = Normalize(
    input_shapes={"observation.state": (10,)},
    normalization_mapping={"observation.state": NormalizationMode.MEAN_STD},
    stats=stats
)

# 训练时
batch = {"observation.state": torch.randn(32, 10)}
norm_batch = normalize(batch)
```

### 7.3 评估策略

```python
from waleo_policy.utils import compute_metrics
from waleo_policy.factory import PolicyFactory

# 加载策略
policy = PolicyFactory.create("act", config)
policy.eval()

# 评估
episode_returns = []
for episode in range(50):
    obs, _ = env.reset()
    done = False
    episode_return = 0

    while not done:
        action = policy.select_action({"observation.state": obs})
        obs, reward, done, truncated, info = env.step(action)
        episode_return += reward

    episode_returns.append(episode_return)

# 计算指标
metrics = compute_metrics(episode_returns)
print(f"Success rate: {metrics['success_rate']:.2%}")
print(f"Average return: {metrics['average_return']:.2f}")
```

---

## 8. 测试策略

### 8.1 单元测试

```python
# tests/test_base_policy.py
import torch
from waleo_policy.base import BasePolicy, PolicyConfig

def test_base_policy():
    """测试 BasePolicy 基类"""

    class DummyPolicy(BasePolicy):
        name = "dummy"
        config_class = PolicyConfig

        def __init__(self, config):
            super().__init__(config)
            self.linear = torch.nn.Linear(10, 6)

        def get_optim_params(self):
            return [{"params": self.parameters()}]

        def reset(self):
            pass

        def forward(self, batch):
            obs = batch["observation.state"]
            action = batch["action"]
            pred = self.linear(obs)
            loss = torch.nn.functional.mse_loss(pred, action)
            return loss, {"loss": loss.item()}

        def select_action(self, batch):
            obs = batch["observation.state"]
            return self.linear(obs)

    config = PolicyConfig(
        name="dummy",
        input_shapes={"observation.state": (10,)},
        output_shapes={"action": (6,)}
    )

    policy = DummyPolicy(config)

    # 测试 forward
    batch = {
        "observation.state": torch.randn(32, 10),
        "action": torch.randn(32, 6)
    }
    loss, logs = policy(batch)
    assert loss.item() >= 0
    assert "loss" in logs

    # 测试 select_action
    action = policy.select_action({"observation.state": torch.randn(1, 10)})
    assert action.shape == (1, 6)

# tests/test_normalize.py
from waleo_policy.normalize import Normalize
from waleo_policy.base import NormalizationMode
import numpy as np

def test_normalize():
    """测试归一化"""

    stats = {
        "observation.state": {
            "mean": np.array([0.0, 0.0]),
            "std": np.array([1.0, 1.0])
        }
    }

    normalize = Normalize(
        input_shapes={"observation.state": (2,)},
        normalization_mapping={"observation.state": NormalizationMode.MEAN_STD},
        stats=stats
    )

    batch = {"observation.state": torch.tensor([[1.0, 2.0]])}
    norm_batch = normalize(batch)

    expected = torch.tensor([[1.0, 2.0]])  # (x - 0) / 1 = x
    assert torch.allclose(norm_batch["observation.state"], expected)

# tests/test_factory.py
from waleo_policy.factory import PolicyFactory

def test_factory():
    """测试策略工厂"""

    # 测试注册
    class TestPolicy(BasePolicy):
        name = "test"
        config_class = PolicyConfig

        def __init__(self, config):
            super().__init__(config)

        def get_optim_params(self):
            return []

        def reset(self):
            pass

        def forward(self, batch):
            return torch.tensor(0.0), {}

        def select_action(self, batch):
            return torch.zeros(1, 1)

    PolicyFactory.register("test", TestPolicy)

    # 测试创建
    config = PolicyConfig(name="test")
    policy = PolicyFactory.create("test", config)
    assert isinstance(policy, TestPolicy)

    # 测试列表
    policies = PolicyFactory.list_policies()
    assert "test" in policies
```

### 8.2 测试覆盖率目标

- 核心功能：> 85%
- 边界情况：> 75%
- 总体：> 80%

---

## 9. 性能考虑

### 9.1 性能目标

- 归一化/反归一化：< 1ms（batch_size=32）
- 策略选择动作：< 10ms（batch_size=32，不含具体模型）

### 9.2 优化策略

- 使用 torch.jit.script 编译归一化层
- 统计信息预加载为 GPU tensor
- 避免不必要的 CPU-GPU 数据传输

---

## 10. 依赖项

### 10.1 核心依赖（必需）

```toml
[tool.poetry.dependencies]
python = "^3.9"
torch = "^2.0.0"
numpy = "^1.24.0"
waleo-utils = { path = "../waleo-utils", develop = true }
waleo-config = { path = "../waleo-config", develop = true }
```

### 10.2 开发依赖

```toml
[tool.poetry.dev-dependencies]
pytest = "^7.4.0"
pytest-cov = "^4.1.0"
black = "^23.0.0"
mypy = "^1.5.0"
```

---

## 11. 兼容性

### 11.1 Python 版本

- 最低：Python 3.9
- 测试：3.9, 3.10, 3.11

### 11.2 平台支持

- Linux: ✅ 完全支持
- macOS: ✅ 支持
- Windows: ✅ 支持

### 11.3 PyTorch 版本

- 最低：PyTorch 2.0.0
- 推荐：PyTorch 2.1.0+

---

## 12. 文件大小估算

| 文件/目录 | 估计行数 | 说明 |
|-----------|----------|------|
| `base/types.py` | ~100 | 数据类型定义 |
| `base/config.py` | ~150 | PolicyConfig 配置基类 |
| `base/policy.py` | ~250 | BasePolicy 核心实现 |
| `normalize/normalize.py` | ~200 | Normalize 类实现 |
| `normalize/unnormalize.py` | ~150 | Unnormalize 类实现 |
| `factory/factory.py` | ~120 | PolicyFactory 实现 |
| `utils/metrics.py` | ~100 | 评估指标 |
| `__init__.py` | ~50 | 导出接口 |
| **总计** | **~1120** | 不含测试 |

---

## 13. 实现优先级

### Phase 1: 核心基类（必须）

1. `base/types.py` - 数据类型定义
2. `base/config.py` - PolicyConfig 配置基类
3. `base/policy.py` - BasePolicy 抽象基类

### Phase 2: 归一化（必须）

4. `normalize/normalize.py` - Normalize 类
5. `normalize/unnormalize.py` - Unnormalize 类

### Phase 3: 工厂（推荐）

6. `factory/factory.py` - PolicyFactory 类

### Phase 4: 工具（可选）

7. `utils/metrics.py` - 评估指标

---

## 14. 迁移指南

从原始 LeRobot 迁移到新模块：

```python
# 原始代码
from lerobot.common.policies.pretrained import PreTrainedPolicy
from lerobot.common.policies.normalize import Normalize, Unnormalize

# 新代码
from waleo_policy.base import BasePolicy
from waleo_policy.normalize import Normalize, Unnormalize

# API 主要变化：
# 1. PreTrainedPolicy → BasePolicy
# 2. 包名从 lerobot 改为 waleo
# 3. 配置系统使用 M02 的统一配置
```

---

## 15. 未来扩展

### 15.1 可能的扩展

- 支持更多归一化模式（如 RobustScaler）
- 支持多模态输入（图像 + 状态）
- 支持条件策略（ conditioned on task）
- 支持分层策略（hierarchical policies）

### 15.2 版本计划

- v0.1.0: 基础功能实现
- v0.2.0: 添加更多归一化模式
- v0.3.0: 支持多模态策略

---

## 16. 变更日志

### v0.1.0 (2026-01-08)

- 初始设计
- 定义 BasePolicy 抽象基类
- 定义归一化工具
- 定义策略工厂
- 确定模块接口和依赖关系
